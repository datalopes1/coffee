version: 2

models:
  - name: stg_excel__sales
    description: "Staging layer com dados padronizados."
    columns:
      - name: transaction_id
        tests:
          - unique
          - not_null
